<!DOCTYPE html>
<html>
  <head>
    <title>RubyProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css' %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    
    <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
      

      <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
        <li><span class="nav-link px-2 link-secondary"><%= link_to "Главная", home_path %></span></li>
        <li><span class="nav-link px-2 link-dark"><%= link_to "Новый пост", new_post_path %></span></li>
        <li><span class="nav-link px-2 link-dark"><%= link_to "Автор",about_path %></span></li>
        <% if logged_in? %>
          <li><span class="nav-link px-2 link-dark"><%= link_to "Мой профиль(#{current_user.username})",profile_path(current_user.username) %></span></li>
        <%end%>
        
      </ul>

      <div class="col-md-3 text-end">
        <% if !logged_in? %>
          <%= link_to "Войти",login_path, {:class => 'btn btn-outline-primary me-2'} %>
          <%= link_to "Регистрация",signup_path, {:class => 'btn btn-primary'} %>
        <% else %>
          <%= link_to "Выйти",logout_path, {:class => 'btn btn-outline-primary me-2'} %>
        <% end %>
      </div>
    </header>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
